spring:
  jpa:
    database: h2
    properties.hibernate.dialect: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

  datasource:
    url: jdbc:h2:mem:testdb;MODE=MYSQL #;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver

  #      fixme url수정
#  data:
#    mongodb:
#      uri: mongodb+srv://wkdrndydtlr:SophistRing123@chat.7p1uuxx.mongodb.net/chat_messages?retryWrites=true&w=majority

jwt:
  secret-key: asnalsdnlansdlalsdlasldnalsndlansdlnalsndlansldnalsnlasnldn

logging:
  level:
    root: info
    org:
      hibernate:
        type: trace
      springframework:
        security: trace